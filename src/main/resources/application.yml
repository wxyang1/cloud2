server:
  port: 8760
eureka:
  instance:
    hostname: localhost
  client:
    # register-with-eureka: false
    # fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/